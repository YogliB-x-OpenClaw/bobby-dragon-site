# bobby-dragon-site

A small, creative playground for Bobby the Dragon. Deployed with GitHub Pages from the 'gh-pages' branch.

Build / Deploy: simple static files in /public. The repo uses an automated push to gh-pages.
